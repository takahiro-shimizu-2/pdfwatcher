{
  "name": "@pdf-watcher/client-gas",
  "version": "1.0.0",
  "description": "Client-side Google Apps Script for PDF Watcher",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "push": "clasp push",
    "deploy": "npm run build && npm run push"
  },
  "dependencies": {
    "@pdf-watcher/core": "file:../core"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.0",
    "@types/google-apps-script": "^1.0.0",
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  }
}